<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>khana kya banau</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Analytics is off unless a token is injected at build time. Supply it with
	     `xcodebuild MIXPANEL_TOKEN=…` or a Secrets.xcconfig; the event names are a
	     wire contract shared with the Android and web clients. -->
	<key>MixpanelToken</key>
	<string>$(MIXPANEL_TOKEN)</string>
	<key>AnalyticsEnabled</key>
	<string>$(ANALYTICS_ENABLED)</string>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<!-- Cream-100, matching KkbBackground so launch doesn't flash white. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<!-- No UIBackgroundModes. Remote push is not shipped in 1.0: the bundled
	     GoogleService-Info.plist is a placeholder, so PushService.configure()
	     bails and never registers for APNs. Declaring the background mode
	     without a matching aps-environment entitlement earns a "Missing Push
	     Notification Entitlement" warning on every upload. Prep reminders are
	     local notifications and need none of this. Restore this array together
	     with App/KhanaKyaBanau-Push.entitlements when push actually ships. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<!--
	  The team prefix, needed to build the fully-qualified keychain access group
	  at runtime: keychain groups are "<prefix>.<group>" and the prefix is not
	  knowable from source. Absent on a build without the entitlement, which is
	  how KeychainStore.sharedAccessGroup decides to stay nil.
	-->
	<key>AppIdentifierPrefix</key>
	<string>$(AppIdentifierPrefix)</string>

	<!-- Opening a saved recipe hands off to the YouTube app when it is installed. -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>youtube</string>
		<string>whatsapp</string>
	</array>
</dict>
</plist>
